{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch API",
            "cwd": "${workspaceFolder}/api",
            "program": "${workspaceFolder}/api/src/app.js"
        }
    ]
}
